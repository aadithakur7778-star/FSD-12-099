

    const toWords = (digit) => {
        const words = ['zero', 'one', 'two', 'three', 'four', 'five', 'six', 'seven', 'eight', 'nine'];
        return words[digit];
    }
    // frontened vuejs react flask  angularjs

    //  css (bootstrap, tailwind, material ui)

    // backend (nodejs,  django, springboot,fastapi,php)