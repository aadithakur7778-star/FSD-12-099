# File System (FS Module)
# CRUD Project

assume we are making a cart related project

1. user can add any product (id,name,price,qty) into cart
2. user can see all the items of the cart 
3. user can  remove the items from the cart 
4. user can also update quantity of  product
5. all the items should be stored after termination of project



# Points
 
 readline is used to take the input from the terminal
 stdin and stdout is for keyboard input and screen output
 readFile / writeFile : file read/write karne ke liye.
 fetCard= File ko read karta hai.
utf-8 means text format me read karo.
JSON.parse(data) se string ko JavaScript object me convert karta hai.
